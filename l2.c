#include <stdio.h>
#include <cs50.h>

int main (void)
{
    string name = get_string("What's your name? ");
    printf("hello, %s\n", name);
}


// pre processing [Normal code]
// compiler [vscode which converts code to assembly code]
// assmebly [assembly code to binary 0's and 1's]
// linking [combines all binary data]
